<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to "Delete", user, class: "btn waves-effect waves-light red",
      method: :delete, data: {confirm: "You sure?"} %>
  <% end %>
</li>
